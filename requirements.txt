# Core dependencies
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
bitsandbytes>=0.41.0  # For quantization
sentencepiece>=0.1.99
protobuf>=3.20.0

# IMPORTANT: NumPy must be < 2.0 for FAISS compatibility
numpy>=1.24.0,<2.0

# Vector stores and retrieval
faiss-cpu>=1.7.2  # Use faiss-cpu for compatibility, upgrade to faiss-gpu if needed
# faiss-gpu>=1.7.2  # Uncomment for GPU version (may have compatibility issues)
chromadb>=0.4.22
langchain>=0.1.0
langchain-community>=0.0.10

# Embedding models
sentence-transformers>=2.2.2
# InstructorEmbedding  # Optional - install separately if needed

# Data processing
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
tiktoken>=0.5.0

# Document processing
pypdf>=3.17.0
python-docx>=1.0.0
markdown>=3.5.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Dataset handling
datasets>=2.14.0
pyarrow>=14.0.0

# API and web (optional)
# fastapi>=0.104.0
# uvicorn>=0.24.0
# httpx>=0.25.0
# aiohttp>=3.9.0
# pydantic>=2.5.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
click>=8.1.0
rich>=13.7.0
tqdm>=4.66.0
tenacity>=8.2.0

# Monitoring and logging
loguru>=0.7.0
# wandb>=0.16.0  # Optional - uncomment if you want experiment tracking
# prometheus-client>=0.19.0  # Optional

# Optional: Gradio for demo UI
# gradio>=4.10.0

# Optional: For web search capability
# duckduckgo-search>=4.1.0